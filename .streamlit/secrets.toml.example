# ============================================================
# Bubble Radar — API Key Configuration
# ============================================================
# 1. COPY this file and rename it to:  secrets.toml
#    (remove the .example extension)
# 2. Replace the placeholder value with your actual API key.
# 3. NEVER commit secrets.toml to version control.
#
# News data uses Google News (free, no API key needed).
# ============================================================

GEMINI_API_KEY = "your-gemini-api-key-here"
